import 'package:flutter/widgets.dart';
import '../classes/device.dart';

class RulePage extends StatelessWidget {
  final Device device;
  const RulePage({super.key, required this.device});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
